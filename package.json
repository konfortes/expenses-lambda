{
  "name": "expenses-lambda",
  "version": "1.0.0",
  "description": "## serverless Workflow",
  "main": "s3.test.js",
  "scripts": {
    "deploy": "cd lambda && serverless deploy --region eu-central-1",
    "logs": "cd lambda && serverless logs -f parse -t",
    "invoke": "cd lambda && serverless invoke local --function parse",
    "test": "jest --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/konfortes/expenses-lambda.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/konfortes/expenses-lambda/issues"
  },
  "homepage": "https://github.com/konfortes/expenses-lambda#readme",
  "devDependencies": {
    "jest": "^24.5.0"
  },
  "dependencies": {
    "aws-sdk": "^2.422.0",
    "xlsx": "^0.14.1"
  }
}
